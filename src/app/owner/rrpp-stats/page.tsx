'use client';

// Al igual que con las otras páginas, reutilizamos el componente que ya existe
// en el dashboard del admin para mantener la consistencia.
import RRPPStatsPage from '@/app/dashboard/rrpp-stats/page';

export default function OwnerRRPPStatsPage() {
  return <RRPPStatsPage />;
}